services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: deploy
    ports:
      - "3000:3000"
    volumes:
      - ./backend:/work
    env_file:
      - ./backend/.env
